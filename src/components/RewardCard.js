import React, { Component } from "react";
import tick from "../images/collected.png";
export class RewardCard extends Component {
  render() {
    return (
      <div className="card2">
      {/* code goes here */}  
      {/* Display reward card,collect button,retry button */}
{/* Kindly use  "RewardCard" as class name for reward card div and and "reward" as id when changing the color of the reward card div. */}
      </div>
    );
  }
}

export default RewardCard;
